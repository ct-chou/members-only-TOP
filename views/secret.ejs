<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Enter Secret Code</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body { font-family: Arial, sans-serif; background: #f4f4f4; }
        .container { max-width: 400px; margin: 80px auto; background: #fff; padding: 2em; border-radius: 8px; box-shadow: 0 2px 8px rgba(0,0,0,0.1);}
        h2 { text-align: center; }
        label, input { display: block; width: 100%; }
        input[type="text"] { margin: 1em 0; padding: 0.5em; }
        button { width: 100%; padding: 0.7em; background: #007bff; color: #fff; border: none; border-radius: 4px; font-size: 1em; }
        button:hover { background: #0056b3; }
    </style>
</head>
<body>
    <div class="container">
        <h2>Enter Secret Code</h2>
        <% if (typeof errors !== 'undefined') { %>
            <div class="error-container">
            <ul>
                <% errors.forEach(error => { %>
                <li><%= error.msg %></li>
                <% }) %>
            </ul>
            </div>
        <% } %>
        <form action="/secret" method="POST">
            <label for="secretCode">Secret Code:</label>
            <input type="text" id="secretCode" name="secretCode" required>
            <button type="submit">Submit</button>
        </form>
        <p style="text-align:center; color: #888; margin-top:1em;">Enter the secret code to gain full membership access.</p>
        <p style="text-align:center; color: #888; margin-top:1em;"><a href="/">Back to Home</a></p>
    </div>
</body>
</html> 